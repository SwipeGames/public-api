---
id: createnewgamerequest
title: "CreateNewGameRequest"
description: ""
sidebar_label: "CreateNewGameRequest"
hide_title: true
hide_table_of_contents: true
schema: true
sample: {"cID":"3fa85f64-5717-4562-b3fc-2c963f66afa6","extCID":"your_ext_id","gameID":"sg_catch_97","sessionID":"a1b2c3d4-e5f6-7890-abcd-ef1234567890","returnURL":"https://your-site.com/game-lobby","depositURL":"https://your-site.com/deposit","currency":"USD","locale":"en_us","fallbackToDefaultLocale":false,"platform":"desktop","demo":true,"initDemoBalance":"10000.00","user":{"id":"string","firstName":"string","lastName":"string","nickName":"string","country":"string"}}
custom_edit_url: null
---

import Schema from "@theme/Schema";
import Heading from "@theme/Heading";

<Heading
  as={"h1"}
  className={"openapi__heading"}
  children={"CreateNewGameRequest"}
>
</Heading>





<Schema
  schema={{"type":"object","required":["cID","extCID","gameID","demo","platform","currency","locale"],"properties":{"cID":{"description":"Client's ID. It's provided by Swipe Games and is used as your identifier.\n","type":"string","format":"uuid","x-go-type":"uuid.UUID","x-go-type-import":{"name":"uuid","path":"github.com/google/uuid"}},"extCID":{"description":"External Client's ID (client/platform/game aggregator/integrator or casino on your side). This is provided from your side, and this is\nhow you identify your clients. We use it in all callbacks to you.\n","type":"string","example":"your_ext_id"},"gameID":{"description":"Game's ID. This is Swipe Games's game identifier. See the list of supported games in [Games](/games) section.\n","type":"string","example":"sg_catch_97"},"sessionID":{"description":"Session ID (external). This is your own Session ID, generated on your side. We store it and echo it back in all callbacks to you.\nNot required for demo games, since we don't use callbacks for demo games.\n\nNote: this is NOT the same as the `gsID` (Game Session ID) returned in the response — `gsID` is Swipe Games's own\nidentifier for the game session and is unrelated to the value you send here.\n","type":"string","example":"a1b2c3d4-e5f6-7890-abcd-ef1234567890"},"returnURL":{"description":"Return URL which we could use to redirect the user after the game is finished/exited. If you don't provide this URL, return button in the game will be disabled. Please make sure you provide this URL when using redirect to open the game, otherwise player cannot go back to your site.","type":"string","example":"https://your-site.com/game-lobby"},"depositURL":{"description":"Deposit URL which we could use to open a deposit page for the user. If you don't provide this URL, deposit flow in the game will be disabled.","type":"string","example":"https://your-site.com/deposit"},"currency":{"description":"Currency code in ISO4217.","type":"string","example":"USD"},"locale":{"description":"Locale code in IETF BCP 47 format (ISO 639-1 language code with optional ISO 3166-1 country code), using underscore as separator.\nSee [Locales](/locales) for the list of supported locales. If the requested locale is not supported the request fails with\n`400 locale_not_supported`, unless `fallbackToDefaultLocale` is set (see below).\n","type":"string","example":"en_us"},"fallbackToDefaultLocale":{"description":"If `true` and the requested `locale` is not supported, the game is created with the default locale (`en_us`)\ninstead of returning a `400 locale_not_supported` error. Defaults to `false`.\n","type":"boolean","default":false,"example":false},"platform":{"description":"Platform code. This is used to identify the platform where the game is launched.","allOf":[{"type":"string","description":"Platform type where the game can be launched","enum":["desktop","mobile"],"title":"PlatformType"}],"example":"desktop"},"demo":{"description":"Demo mode. If true, the game will be launched in demo mode (means no callbacks will be fired on your side,\nand we use our demo balance for the game). If false, the game will be launched in real mode.\n","type":"boolean"},"initDemoBalance":{"description":"Initial demo balance for the user (in currency units). Only used in demo mode (`demo: true`),\nignored in real mode. Must be greater than the game's minimum bet, otherwise the request fails\nwith `400`. If omitted, the demo balance defaults to 10 000 USD converted into the requested `currency`.\n\n**Applied once, when the demo balance is created.** The demo balance is stored per\n(`cID`, `extCID`, `user.id`, `currency`). `initDemoBalance` is used only when no demo balance\nexists yet for that combination.\n\n**For an existing user the value is ignored.** If the user already has a demo balance for the same\n`cID`, `extCID` and `currency`, the game starts with the balance left from the previous demo games.\nSending a different `initDemoBalance` does not reset, top up, or overwrite that balance.\n\n**There is no Public API operation to change the demo balance after it is created.** To start a demo\ngame from a specific balance, send a `user.id` that has no demo balance yet for this `currency`, or\nomit `user` — for demo games we generate a new user ID on every call, so `initDemoBalance` always applies.\n","type":"string","example":"10000.00"},"user":{"description":"User information we show in the game. User should be provided for real game.\n","x-go-type":"User","type":"object","required":["id"],"properties":{"id":{"description":"User's ID (external). This is User's ID on your side.","type":"string"},"firstName":{"type":"string"},"lastName":{"type":"string"},"nickName":{"type":"string"},"country":{"description":"Country code ISO 3166-1 alpha-2.","type":"string"}},"title":"User"}},"title":"CreateNewGameRequest"}}
  schemaType={"response"}
>
  
</Schema>
            